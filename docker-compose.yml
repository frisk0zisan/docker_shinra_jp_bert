version: "3"
services:
  app:
    container_name: "ishii_container"
    build:
      context: .
      dockerfile: ./Dockerfile
    image: ishii_container
    volumes:
        - /home/masaki:/home/
    tty: true
